# v1.1.0-pre-alpha

- Added camera static to the menu
- Changed naming convention of xyz_manager from ``:addXyz`` ``:setCurrentXyz`` to ``:add`` ``:setCurrent``
- Added SFX manager
- Added fade when you press "New game" or "Continue X"
- Added a warning screen when booting up the game
- Put all of the managers inside ``globals.lua`` so another scene can switch to another scene